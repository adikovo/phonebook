{
  "name": "phonebook",
  "version": "0.1.0",
  "private": true,
  "description": "A single-user phonebook web app for managing personal contacts.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
